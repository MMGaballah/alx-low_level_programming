#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
int a;
for (i = 48; i < 58; i++)
{
putchar(i);
}
for (a = 97; a < 103; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
