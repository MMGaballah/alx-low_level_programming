#include "main.h"
/**
 * add - adds two integers.
 * @a: first integer added.
 * @b: second integer added.
 *
 * Return: result of addition.
 */
int add(int a, int b)
{
int n;
n = a + b;
return (n);
}
